platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfcli/cli-base

inputs:
 - name: updated-translation-files

outputs:
 - name: output

run:
  path: bash
  args:
  - -c
  - |
    set -ex

    zip -cvf outputs/all.json.zip updated-translation-files/*all.json
